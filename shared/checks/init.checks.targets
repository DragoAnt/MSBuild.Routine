<Project>

  <Import Project="$(MSBuildThisFileDirectory)check.shared.properties.targets" />

  <Import Project="$(MSBuildThisFileDirectory)check.prerelease.packages.targets" />

  <Import Project="$(MSBuildThisFileDirectory)check.restrict.reference.targets" />

</Project>